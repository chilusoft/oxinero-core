# Monexo / Oxinero – root .env (optional)
# Copy to .env and set values. Used by deploy script and frontend build.

# API URL for production frontend build (deploy sets VITE_API_BASE_URL from this)
REMOTE_API_URL=https://oxinero.chilusoft.dev/backend/api
